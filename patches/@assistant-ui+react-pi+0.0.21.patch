diff --git a/node_modules/@assistant-ui/react-pi/dist/runtime/usePiRuntime.js b/node_modules/@assistant-ui/react-pi/dist/runtime/usePiRuntime.js
index b84f424..68d7c6c 100644
--- a/node_modules/@assistant-ui/react-pi/dist/runtime/usePiRuntime.js
+++ b/node_modules/@assistant-ui/react-pi/dist/runtime/usePiRuntime.js
@@ -6,6 +6,7 @@ import { PiThreadController, appendMessageParts } from "./ThreadController.js";
 import { piExtras } from "./piExtras.js";
 import { ExportedMessageRepository, useAui, useAuiState, useExternalStoreRuntime, useRemoteThreadListRuntime } from "@assistant-ui/react";
 import { invokeUserCallback } from "@assistant-ui/core/internal";
+import { createAssistantStream } from "assistant-stream";
 import { useCallback, useEffect, useEffectEvent, useMemo, useRef, useState, useSyncExternalStore } from "react";
 //#region src/runtime/usePiRuntime.ts
 const EMPTY_THREAD_STATE = createPiThreadState("__pending__");
@@ -310,9 +311,15 @@ const usePiRuntime = (options) => {
 				externalId: snapshot.metadata.id
 			};
 		},
-		generateTitle: async () => new ReadableStream({ start(streamController) {
-			streamController.close();
-		} }),
+		generateTitle: async (remoteId, messages) => {
+			const firstUserMessage = messages.find((message) => message.role === "user");
+			const text = firstUserMessage?.content.find((part) => part.type === "text")?.text.trim();
+			const title = !text ? "New Task" : text.length > 50 ? `${text.slice(0, 47)}...` : text;
+			await client.renameThread(remoteId, title);
+			return createAssistantStream((controller) => {
+				controller.appendText(title);
+			});
+		},
 		fetch: async (threadId) => {
 			const snapshot = await client.getThread(threadId);
 			return mapThreadMetadata(snapshot.metadata);
diff --git a/node_modules/@assistant-ui/react-pi/src/runtime/usePiRuntime.ts b/node_modules/@assistant-ui/react-pi/src/runtime/usePiRuntime.ts
index 6a68d30..93d67da 100644
--- a/node_modules/@assistant-ui/react-pi/src/runtime/usePiRuntime.ts
+++ b/node_modules/@assistant-ui/react-pi/src/runtime/usePiRuntime.ts
@@ -15,6 +15,7 @@ import type {
   ThreadMessageLike,
 } from "@assistant-ui/react";
 import { invokeUserCallback } from "@assistant-ui/core/internal";
+import { createAssistantStream } from "assistant-stream";
 import {
   useEffect,
   useEffectEvent,
@@ -547,14 +548,25 @@ export const usePiRuntime = (options: PiRuntimeOptions): AssistantRuntime => {
           externalId: snapshot.metadata.id,
         };
       },
-      generateTitle: async () =>
-        // Pi has no server-side title summarization; titles come from
-        // `session_info_changed`. Satisfy the contract with an empty stream.
-        new ReadableStream({
-          start(streamController) {
-            streamController.close();
-          },
-        }) as never,
+      generateTitle: async (remoteId: string, messages: readonly ThreadMessage[]) => {
+        const firstUserMessage = messages.find(
+          (message) => message.role === "user",
+        );
+        const text = firstUserMessage?.content
+          .find((part) => part.type === "text")
+          ?.text.trim();
+        const title = !text
+          ? "New Task"
+          : text.length > 50
+            ? `${text.slice(0, 47)}...`
+            : text;
+
+        await client.renameThread(remoteId, title);
+
+        return createAssistantStream((controller) => {
+          controller.appendText(title);
+        });
+      },
       fetch: async (threadId: string) => {
         const snapshot = await client.getThread(threadId);
         return mapThreadMetadata(snapshot.metadata);
